// Cobra is small, explicit, and built to grow.
fn fibonacci(n) {
    if n < 2 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

let answer = fibonacci(10);
print("Cobra online.");
print("fibonacci(10) =", answer);
print("type:", type_of(answer));