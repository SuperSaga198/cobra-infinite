# Cobra Federation node map.
# The first release runs locally; these fields define the stable shape for
# future Earth/Mars synchronization without coupling the compiler to a network.

[earth]
tick_rate_hz = 1
latency_budget_ms = 0
role = "primary-development"

[mars]
tick_rate_hz = 1
latency_budget_ms = 225000
role = "planned-replica"